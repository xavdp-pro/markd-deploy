# CI workflow disabled - no tests configured yet
# Re-enable when tests and lint are set up
name: CI

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - run: echo "CI disabled - enable when tests are ready"
